package com.revature.hal_abs;

public abstract class Shape_hw {
    protected abstract double calculateArea(int x, int y);

    //* abstract methods *//
    protected abstract double calculateArea(double radius);

    protected abstract double calculateCircumference(double z);

    protected abstract double calculateArea(double height, double width);

    protected abstract double calculateCircumference(double height, double width);
}
